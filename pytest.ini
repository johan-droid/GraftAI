[pytest]
# Limit discovery to backend tests to avoid collecting example/test output files
testpaths = backend/tests
addopts = -q

# Register common markers used in the repo
markers =
    integration: integration tests
    api: api tests
    unit: unit tests
    ai: ai-related unit tests
    slow: slow tests
