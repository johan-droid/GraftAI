{
  "name": "graftai-monorepo",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/dev-concurrent.js",
    "dev-all": "node ./scripts/dev-concurrent.js",
    "dev-concurrent": "node ./scripts/dev-concurrent.js",
    "verify": "node ./scripts/verify-setup.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
