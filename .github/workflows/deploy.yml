name: GraftAI Production Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-and-deploy-backend:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install pytest pytest-asyncio httpx

      - name: Run Integration Tests (CRITICAL GATE)
        env:
          ENV: testing
          # Add any mock API keys needed for tests here
        run: |
          cd backend
          pytest -v tests/integration/

      # ONLY RUNS IF TESTS PASS GREEN
      - name: Deploy to Render
        env:
          deploy_url: ${{ secrets['RENDER_DEPLOY_HOOK_URL'] }}
        run: |
          if [ -z "$deploy_url" ]; then
            echo "Skipping deployment: RENDER_DEPLOY_HOOK_URL secret not configured"
            exit 0
          fi
          curl -fS --show-error "$deploy_url"
